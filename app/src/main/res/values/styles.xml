<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MovieRatingBar" parent="@android:style/Widget.DeviceDefault.Light.RatingBar.Indicator">
    <!--
    <item name="android:background">@color/colorRatingBackground</item>
    <item name="android:secondaryProgress">@color/colorRatingSecondary</item>
    <item name="android:progress">@color/colorRatingColor</item>
    -->
    </style>

    <!-- Sourced from https://developer.android.com/guide/topics/ui/themes.html -->
    <style name="BaseText" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorSelectorText</item>
        <item name="android:ellipsize">none</item>
    </style>

    <style name="MovieTitle" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">45sp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorSelectorText</item>
    </style>

</resources>
